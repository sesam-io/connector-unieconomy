{
  "_id": "{{@ system @}}",
  "oauth2": {
    "access_token": "$SECRET(oauth_access_token)",
    "client_id": "$SECRET(oauth_client_id)",
    "client_secret": "$SECRET(oauth_client_secret)",
    "refresh_token": "$SECRET(oauth_refresh_token)",
    "token_url": "{{@ token_url @}}"
  },
  "operations": {
    "companies-delete": {
      "method": "DELETE",
      "url": "api/biz/companies/{{ entity['$source']['ID'] }}"
    },
    "companies-insert": {
      "method": "POST",
      "payload-type": "json",
      "url": "api/biz/companies/{{ entity['$source']['ID'] }}"
    },
    "companies-list": {
      "id_expression": "{{ ID }}",
      "method": "GET",
      "next_page_link": "api/biz/companies",
      "next_page_termination_strategy": [
        "same-response",
        "empty-result"
      ],
      "params": {
        "filter": "{% if since is defined and since is not none %}(UpdatedAt ge '{{ since }}' or CreatedAt ge '{{ since }}'){% else %}sesam:markskip{% endif %}",
        "orderby": "ID",
        "skip": "{% if is_first_page %}sesam:markskip{% else %}{{ 1000|int*page }}{% endif %}",
        "top": "1000"
      },
      "since_property_location": "manual",
      "updated_expression": "{% if entity.UpdatedAt is defined and entity.UpdatedAt %}{{ entity.UpdatedAt }}{% else %}{%if entity.CreatedAt is defined and entity.CreatedAt %}{{ entity.CreatedAt }}{% else %}1970-01-01T00:00:00{% endif %}{% endif %}",
      "url": "api/biz/companies"
    },
    "companies-lookup": {
      "method": "GET",
      "url": "api/biz/companies/{{ entity['$source']['ID'] }}"
    },
    "companies-update": {
      "method": "PUT",
      "payload-type": "json",
      "url": "api/biz/companies/{{ entity['$source']['ID'] }}"
    },
    "contacts-delete": {
      "method": "DELETE",
      "url": "api/biz/contacts/{{ entity['$source']['ID'] }}"
    },
    "contacts-insert": {
      "method": "POST",
      "payload-type": "json",
      "url": "api/biz/contacts/{{ entity['$source']['ID'] }}"
    },
    "contacts-list": {
      "id_expression": "{{ ID }}",
      "method": "GET",
      "next_page_link": "api/biz/contacts",
      "next_page_termination_strategy": [
        "same-response",
        "empty-result"
      ],
      "params": {
        "filter": "{% if since is defined and since is not none %}(UpdatedAt ge '{{ since }}' or CreatedAt ge '{{ since }}'){% else %}sesam:markskip{% endif %}",
        "orderby": "ID",
        "skip": "{% if is_first_page %}sesam:markskip{% else %}{{ 1000|int*page }}{% endif %}",
        "top": "1000"
      },
      "since_property_location": "manual",
      "updated_expression": "{% if entity.UpdatedAt is defined and entity.UpdatedAt %}{{ entity.UpdatedAt }}{% else %}{%if entity.CreatedAt is defined and entity.CreatedAt %}{{ entity.CreatedAt }}{% else %}1970-01-01T00:00:00{% endif %}{% endif %}",
      "url": "api/biz/contacts?expand=Info,Info.InvoiceAddress,Info.DefaultPhone,Info.DefaultEmail,Info.DefaultAddress&hateoas=false&top=100"
    },
    "contacts-lookup": {
      "method": "GET",
      "url": "api/biz/contacts/{{ entity['$source']['ID'] }}?expand=Info,Info.InvoiceAddress,Info.DefaultPhone,Info.DefaultEmail,Info.DefaultAddress"
    },
    "contacts-update": {
      "method": "PUT",
      "payload-type": "json",
      "url": "api/biz/contacts/{{ entity['$source']['ID'] }}"
    },
    "countries-delete": {
      "method": "DELETE",
      "url": "api/biz/countries/{{ entity['$source']['ID'] }}"
    },
    "countries-insert": {
      "method": "POST",
      "payload-type": "json",
      "url": "api/biz/countries/{{ entity['$source']['ID'] }}"
    },
    "countries-list": {
      "id_expression": "{{ ID }}",
      "method": "GET",
      "next_page_link": "api/biz/countries",
      "next_page_termination_strategy": [
        "same-response",
        "empty-result"
      ],
      "params": {
        "orderby": "ID",
        "skip": "{% if is_first_page %}sesam:markskip{% else %}{{ 1000|int*page }}{% endif %}",
        "top": "1000"
      },
      "url": "api/biz/countries"
    },
    "countries-lookup": {
      "method": "GET",
      "url": "api/biz/countries/{{ entity['$source']['ID'] }}"
    },
    "countries-update": {
      "method": "PUT",
      "payload-type": "json",
      "url": "api/biz/countries/{{ entity['$source']['ID'] }}"
    },
    "currencies-delete": {
      "method": "DELETE",
      "url": "api/biz/currencies/{{ entity['$source']['ID'] }}"
    },
    "currencies-insert": {
      "method": "POST",
      "payload-type": "json",
      "url": "api/biz/currencies/{{ entity['$source']['ID'] }}"
    },
    "currencies-list": {
      "id_expression": "{{ ID }}",
      "method": "GET",
      "next_page_link": "api/biz/currencies",
      "next_page_termination_strategy": [
        "same-response",
        "empty-result"
      ],
      "params": {
        "orderby": "ID",
        "skip": "{% if is_first_page %}sesam:markskip{% else %}{{ 1000|int*page }}{% endif %}",
        "top": "1000"
      },
      "url": "api/biz/currencies"
    },
    "currencies-lookup": {
      "method": "GET",
      "url": "api/biz/currencies/{{ entity['$source']['ID'] }}"
    },
    "currencies-update": {
      "method": "PUT",
      "payload-type": "json",
      "url": "api/biz/currencies/{{ entity['$source']['ID'] }}"
    },
    "currencycodes-delete": {
      "method": "DELETE",
      "url": "api/biz/currencycodes/{{ entity['$source']['ID'] }}"
    },
    "currencycodes-insert": {
      "method": "POST",
      "payload-type": "json",
      "url": "api/biz/currencycodes/{{ entity['$source']['ID'] }}"
    },
    "currencycodes-list": {
      "id_expression": "{{ ID }}",
      "method": "GET",
      "next_page_link": "api/biz/currencycodes",
      "next_page_termination_strategy": [
        "same-response",
        "empty-result"
      ],
      "params": {
        "filter": "{% if since is defined and since is not none %}(UpdatedAt ge '{{ since }}' or CreatedAt ge '{{ since }}'){% else %}sesam:markskip{% endif %}",
        "orderby": "ID",
        "skip": "{% if is_first_page %}sesam:markskip{% else %}{{ 1000|int*page }}{% endif %}",
        "top": "1000"
      },
      "since_property_location": "manual",
      "updated_expression": "{% if entity.UpdatedAt is defined and entity.UpdatedAt %}{{ entity.UpdatedAt }}{% else %}{%if entity.CreatedAt is defined and entity.CreatedAt %}{{ entity.CreatedAt }}{% else %}1970-01-01T00:00:00{% endif %}{% endif %}",
      "url": "api/biz/currencycodes"
    },
    "currencycodes-lookup": {
      "method": "GET",
      "url": "api/biz/currencycodes/{{ entity['$source']['ID'] }}"
    },
    "currencycodes-update": {
      "method": "PUT",
      "payload-type": "json",
      "url": "api/biz/currencycodes/{{ entity['$source']['ID'] }}"
    },
    "customers-delete": {
      "method": "DELETE",
      "url": "api/biz/customers/{{ entity['$source']['ID'] }}"
    },
    "customers-insert": {
      "method": "POST",
      "payload-type": "json",
      "url": "api/biz/customers/{{ entity['$source']['ID'] }}"
    },
    "customers-list": {
      "id_expression": "{{ ID }}",
      "method": "GET",
      "next_page_link": "api/biz/customers",
      "next_page_termination_strategy": [
        "same-response",
        "empty-result"
      ],
      "params": {
        "expand": "Account,BusinessRelation,Info,Info.Phones,Info.DefaultPhone,Info.Addresses,Info.Emails,Info.DefaultEmail,Info.ShippingAddress,Info.InvoiceAddress,Info.DefaultBankAccount,Info.Contacts.Info,Info.Contacts.Info.DefaultEmail,Info.Contacts.Info.DefaultPhone",
        "filter": "{% if since is defined and since is not none %}(UpdatedAt ge '{{ since }}' or CreatedAt ge '{{ since }}'){% else %}sesam:markskip{% endif %}",
        "orderby": "ID",
        "skip": "{% if is_first_page %}sesam:markskip{% else %}{{ 1000|int*page }}{% endif %}",
        "top": "1000"
      },
      "since_property_location": "manual",
      "updated_expression": "{% if entity.UpdatedAt is defined and entity.UpdatedAt %}{{ entity.UpdatedAt }}{% else %}{%if entity.CreatedAt is defined and entity.CreatedAt %}{{ entity.CreatedAt }}{% else %}1970-01-01T00:00:00{% endif %}{% endif %}",
      "url": "api/biz/customers"
    },
    "customers-lookup": {
      "method": "GET",
      "params": {
        "expand": "Account,BusinessRelation,Info,Info.Phones,Info.DefaultPhone,Info.Addresses,Info.Emails,Info.DefaultEmail,Info.ShippingAddress,Info.InvoiceAddress,Info.DefaultBankAccount,Info.Contacts.Info,Info.Contacts.Info.DefaultEmail,Info.Contacts.Info.DefaultPhone"
      },
      "url": "api/biz/customers/{{ entity['$source']['ID']}}"
    },
    "customers-update": {
      "method": "PUT",
      "payload-type": "json",
      "url": "api/biz/customers/{{ entity['$source']['ID']}}"
    },
    "departments-delete": {
      "method": "DELETE",
      "url": "api/biz/departments/{{ entity['$source']['ID'] }}"
    },
    "departments-insert": {
      "method": "POST",
      "payload-type": "json",
      "url": "api/biz/departments/{{ entity['$source']['ID'] }}"
    },
    "departments-list": {
      "id_expression": "{{ ID }}",
      "method": "GET",
      "next_page_link": "api/biz/departments",
      "next_page_termination_strategy": [
        "same-response",
        "empty-result"
      ],
      "params": {
        "filter": "{% if since is defined and since is not none %}(UpdatedAt ge '{{ since }}' or CreatedAt ge '{{ since }}'){% else %}sesam:markskip{% endif %}",
        "orderby": "ID",
        "skip": "{% if is_first_page %}sesam:markskip{% else %}{{ 1000|int*page }}{% endif %}",
        "top": "1000"
      },
      "since_property_location": "manual",
      "updated_expression": "{% if entity.UpdatedAt is defined and entity.UpdatedAt %}{{ entity.UpdatedAt }}{% else %}{%if entity.CreatedAt is defined and entity.CreatedAt %}{{ entity.CreatedAt }}{% else %}1970-01-01T00:00:00{% endif %}{% endif %}",
      "url": "api/biz/departments"
    },
    "departments-lookup": {
      "method": "GET",
      "url": "api/biz/departments/{{ entity['$source']['ID'] }}"
    },
    "departments-update": {
      "method": "PUT",
      "payload-type": "json",
      "url": "api/biz/departments/{{ entity['$source']['ID'] }}"
    },
    "dimensions-delete": {
      "method": "DELETE",
      "url": "api/biz/dimensions/{{ entity['$source']['ID'] }}"
    },
    "dimensions-insert": {
      "method": "POST",
      "payload-type": "json",
      "url": "api/biz/dimensions/{{ entity['$source']['ID'] }}"
    },
    "dimensions-list": {
      "id_expression": "{{ ID }}",
      "method": "GET",
      "next_page_link": "api/biz/dimensions",
      "next_page_termination_strategy": [
        "same-response",
        "empty-result"
      ],
      "params": {
        "filter": "{% if since is defined and since is not none %}(UpdatedAt ge '{{ since }}' or CreatedAt ge '{{ since }}'){% else %}sesam:markskip{% endif %}",
        "orderby": "ID",
        "skip": "{% if is_first_page %}sesam:markskip{% else %}{{ 1000|int*page }}{% endif %}",
        "top": "1000"
      },
      "since_property_location": "manual",
      "updated_expression": "{% if entity.UpdatedAt is defined and entity.UpdatedAt %}{{ entity.UpdatedAt }}{% else %}{%if entity.CreatedAt is defined and entity.CreatedAt %}{{ entity.CreatedAt }}{% else %}1970-01-01T00:00:00{% endif %}{% endif %}",
      "url": "api/biz/dimensions"
    },
    "dimensions-lookup": {
      "method": "GET",
      "url": "api/biz/dimensions/{{ entity['$source']['ID'] }}"
    },
    "dimensions-update": {
      "method": "PUT",
      "payload-type": "json",
      "url": "api/biz/dimensions/{{ entity['$source']['ID'] }}"
    },
    "employees-delete": {
      "method": "DELETE",
      "url": "api/biz/employees/{{ entity['$source']['ID'] }}"
    },
    "employees-insert": {
      "method": "POST",
      "payload-type": "json",
      "url": "api/biz/employees/{{ entity['$source']['ID'] }}"
    },
    "employees-list": {
      "id_expression": "{{ ID }}",
      "method": "GET",
      "next_page_link": "api/biz/employees",
      "next_page_termination_strategy": [
        "same-response",
        "empty-result"
      ],
      "params": {
        "filter": "{% if since is defined and since is not none %}(UpdatedAt ge '{{ since }}' or CreatedAt ge '{{ since }}'){% else %}sesam:markskip{% endif %}",
        "orderby": "ID",
        "skip": "{% if is_first_page %}sesam:markskip{% else %}{{ 1000|int*page }}{% endif %}",
        "top": "1000"
      },
      "since_property_location": "manual",
      "updated_expression": "{% if entity.UpdatedAt is defined and entity.UpdatedAt %}{{ entity.UpdatedAt }}{% else %}{%if entity.CreatedAt is defined and entity.CreatedAt %}{{ entity.CreatedAt }}{% else %}1970-01-01T00:00:00{% endif %}{% endif %}",
      "url": "api/biz/employees"
    },
    "employees-lookup": {
      "method": "GET",
      "url": "api/biz/employees/{{ entity['$source']['ID'] }}"
    },
    "employees-update": {
      "method": "PUT",
      "payload-type": "json",
      "url": "api/biz/employees/{{ entity['$source']['ID'] }}"
    },
    "orderitems-delete": {
      "method": "DELETE",
      "url": "api/biz/orderitems/{{ entity['$source']['ID'] }}"
    },
    "orderitems-insert": {
      "method": "POST",
      "payload-type": "json",
      "url": "api/biz/orderitems/{{ entity['$source']['ID'] }}"
    },
    "orderitems-list": {
      "id_expression": "{{ ID }}",
      "method": "GET",
      "next_page_link": "api/biz/orderitems",
      "next_page_termination_strategy": [
        "same-response",
        "empty-result"
      ],
      "params": {
        "filter": "{% if since is defined and since is not none %}(UpdatedAt ge '{{ since }}' or CreatedAt ge '{{ since }}'){% else %}sesam:markskip{% endif %}",
        "orderby": "ID",
        "skip": "{% if is_first_page %}sesam:markskip{% else %}{{ 1000|int*page }}{% endif %}",
        "top": "1000"
      },
      "since_property_location": "manual",
      "updated_expression": "{% if entity.UpdatedAt is defined and entity.UpdatedAt %}{{ entity.UpdatedAt }}{% else %}{%if entity.CreatedAt is defined and entity.CreatedAt %}{{ entity.CreatedAt }}{% else %}1970-01-01T00:00:00{% endif %}{% endif %}",
      "url": "api/biz/orderitems"
    },
    "orderitems-lookup": {
      "method": "GET",
      "url": "api/biz/orderitems/{{ entity['$source']['ID'] }}"
    },
    "orderitems-update": {
      "method": "PUT",
      "payload-type": "json",
      "url": "api/biz/orderitems/{{ entity['$source']['ID'] }}"
    },
    "orders-delete": {
      "method": "DELETE",
      "url": "api/biz/orders/{{ entity['$source']['ID'] }}"
    },
    "orders-insert": {
      "method": "POST",
      "payload-type": "json",
      "url": "api/biz/orders/{{ entity['$source']['ID'] }}"
    },
    "orders-list": {
      "id_expression": "{{ ID }}",
      "method": "GET",
      "next_page_link": "api/biz/orders",
      "next_page_termination_strategy": [
        "same-response",
        "empty-result"
      ],
      "params": {
        "filter": "{% if since is defined and since is not none %}(UpdatedAt ge '{{ since }}' or CreatedAt ge '{{ since }}'){% else %}sesam:markskip{% endif %}",
        "orderby": "ID",
        "skip": "{% if is_first_page %}sesam:markskip{% else %}{{ 1000|int*page }}{% endif %}",
        "top": "1000"
      },
      "since_property_location": "manual",
      "updated_expression": "{% if entity.UpdatedAt is defined and entity.UpdatedAt %}{{ entity.UpdatedAt }}{% else %}{%if entity.CreatedAt is defined and entity.CreatedAt %}{{ entity.CreatedAt }}{% else %}1970-01-01T00:00:00{% endif %}{% endif %}",
      "url": "api/biz/orders"
    },
    "orders-lookup": {
      "method": "GET",
      "url": "api/biz/orders/{{ entity['$source']['ID'] }}"
    },
    "orders-update": {
      "method": "PUT",
      "payload-type": "json",
      "url": "api/biz/orders/{{ entity['$source']['ID'] }}"
    },
    "products-delete": {
      "method": "DELETE",
      "url": "api/biz/products/{{ entity['$source']['ID'] }}"
    },
    "products-insert": {
      "method": "POST",
      "payload-type": "json",
      "url": "api/biz/products/{{ entity['$source']['ID'] }}"
    },
    "products-list": {
      "id_expression": "{{ ID }}",
      "method": "GET",
      "next_page_link": "api/biz/products",
      "next_page_termination_strategy": [
        "same-response",
        "empty-result"
      ],
      "params": {
        "filter": "{% if since is defined and since is not none %}(UpdatedAt ge '{{ since }}' or CreatedAt ge '{{ since }}'){% else %}sesam:markskip{% endif %}",
        "orderby": "ID",
        "skip": "{% if is_first_page %}sesam:markskip{% else %}{{ 1000|int*page }}{% endif %}",
        "top": "1000"
      },
      "since_property_location": "manual",
      "updated_expression": "{% if entity.UpdatedAt is defined and entity.UpdatedAt %}{{ entity.UpdatedAt }}{% else %}{%if entity.CreatedAt is defined and entity.CreatedAt %}{{ entity.CreatedAt }}{% else %}1970-01-01T00:00:00{% endif %}{% endif %}",
      "url": "api/biz/products"
    },
    "products-lookup": {
      "method": "GET",
      "url": "api/biz/products/{{ entity['$source']['ID'] }}"
    },
    "products-update": {
      "method": "PUT",
      "payload-type": "json",
      "url": "api/biz/products/{{ entity['$source']['ID'] }}"
    },
    "projects-delete": {
      "method": "DELETE",
      "url": "api/biz/projects/{{ entity['$source']['ID'] }}"
    },
    "projects-insert": {
      "method": "POST",
      "payload-type": "json",
      "url": "api/biz/projects/{{ entity['$source']['ID'] }}"
    },
    "projects-list": {
      "id_expression": "{{ ID }}",
      "method": "GET",
      "next_page_link": "api/biz/projects",
      "next_page_termination_strategy": [
        "same-response",
        "empty-result"
      ],
      "params": {
        "filter": "{% if since is defined and since is not none %}(UpdatedAt ge '{{ since }}' or CreatedAt ge '{{ since }}'){% else %}sesam:markskip{% endif %}",
        "orderby": "ID",
        "skip": "{% if is_first_page %}sesam:markskip{% else %}{{ 1000|int*page }}{% endif %}",
        "top": "1000"
      },
      "since_property_location": "manual",
      "updated_expression": "{% if entity.UpdatedAt is defined and entity.UpdatedAt %}{{ entity.UpdatedAt }}{% else %}{%if entity.CreatedAt is defined and entity.CreatedAt %}{{ entity.CreatedAt }}{% else %}1970-01-01T00:00:00{% endif %}{% endif %}",
      "url": "api/biz/projects"
    },
    "projects-lookup": {
      "method": "GET",
      "url": "api/biz/projects/{{ entity['$source']['ID'] }}"
    },
    "projects-update": {
      "method": "PUT",
      "payload-type": "json",
      "url": "api/biz/projects/{{ entity['$source']['ID'] }}"
    },
    "projectstasks-delete": {
      "method": "DELETE",
      "url": "api/biz/projects-tasks/{{ entity['$source']['ID'] }}"
    },
    "projectstasks-insert": {
      "method": "POST",
      "payload-type": "json",
      "url": "api/biz/projects-tasks/{{ entity['$source']['ID'] }}"
    },
    "projectstasks-list": {
      "id_expression": "{{ ID }}",
      "method": "GET",
      "next_page_link": "api/biz/projects-tasks",
      "next_page_termination_strategy": [
        "same-response",
        "empty-result"
      ],
      "params": {
        "filter": "{% if since is defined and since is not none %}(UpdatedAt ge '{{ since }}' or CreatedAt ge '{{ since }}'){% else %}sesam:markskip{% endif %}",
        "orderby": "ID",
        "skip": "{% if is_first_page %}sesam:markskip{% else %}{{ 1000|int*page }}{% endif %}",
        "top": "1000"
      },
      "since_property_location": "manual",
      "updated_expression": "{% if entity.UpdatedAt is defined and entity.UpdatedAt %}{{ entity.UpdatedAt }}{% else %}{%if entity.CreatedAt is defined and entity.CreatedAt %}{{ entity.CreatedAt }}{% else %}1970-01-01T00:00:00{% endif %}{% endif %}",
      "url": "api/biz/projects-tasks"
    },
    "projectstasks-lookup": {
      "method": "GET",
      "url": "api/biz/projects-tasks/{{ entity['$source']['ID'] }}"
    },
    "projectstasks-update": {
      "method": "PUT",
      "payload-type": "json",
      "url": "api/biz/projects-tasks/{{ entity['$source']['ID'] }}"
    }
  },
  "type": "system:rest",
  "url_pattern": "https://test.softrig.com/%s",
  "verify_ssl": true
}